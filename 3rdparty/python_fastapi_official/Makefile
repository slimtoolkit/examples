default: build_fat

build_fat:
	'$(CURDIR)/_slim/build_fat.sh'

slim:
	'$(CURDIR)/_slim/slim.sh'

slim_dockerized:
	'$(CURDIR)/_slim/slim_dockerized.sh'

run_fat:
	'$(CURDIR)/_slim/run_fat.sh'

run_slim:
	'$(CURDIR)/_slim/run_slim.sh'

test:
	'$(CURDIR)/_slim/test.sh'

stop:
	'$(CURDIR)/_slim/stop.sh'

cleanup:
	'$(CURDIR)/_slim/cleanup.sh'

run_debug_sidecar:
	'$(CURDIR)/_slim/run_debug_sidecar.sh'

exec_shell:
	'$(CURDIR)/_slim/exec_shell.sh'

build_and_slim:
	'$(CURDIR)/_slim/build_and_slim.sh'

.PHONY: default build_fat slim slim_dockerized run_fat run_slim test stop cleanup run_debug_sidecar exec_shell build_and_slim
